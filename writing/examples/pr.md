# PR example

ILLUSTRATIVE: this is not an approved sample of the user's voice. Preserve the concrete trigger, changed behavior, and honest verification; do not reuse its facts or test claim.

Title: Reject expired reset links before opening the password form

Expired links currently open the form and fail on submit. The page now checks the token first and shows the existing expired-link message.

Tested: the expired-link and valid-link browser cases passed locally.

Avoid: "This enhancement leverages a robust validation mechanism to seamlessly streamline the user experience." The problem is missing information and exaggerated tone, not a forbidden-word list.
